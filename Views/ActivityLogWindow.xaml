<Window x:Class="Sleipnir.App.Views.ActivityLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Issue Activity Log" Height="500" Width="450"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Border Background="#1A1D2E" CornerRadius="15" BorderBrush="#30363D" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,15">
                <TextBlock Text="📋" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="ISSUE ACTIVITY LOG" FontSize="14" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>
            
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <Grid>
                    <TextBlock Text="No activity logs yet." FontSize="11" Foreground="#8B949E" 
                               HorizontalAlignment="Center" VerticalAlignment="Center" 
                               Margin="0,50,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Items.Count, ElementName=LogsList}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <ItemsControl x:Name="LogsList">
                        <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#30363D" BorderThickness="0,0,0,1" Padding="0,8">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" 
                                                   FontSize="10" Foreground="#8B949E"/>
                                        <TextBlock Text="{Binding UserName}" 
                                                   FontSize="10" FontWeight="Bold" Foreground="#7C4DFF" 
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                    <TextBlock Text="{Binding FormattedLogContent}" 
                                               FontSize="11" Foreground="White" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>

            <Button Grid.Row="2" Content="Close" Click="Close_Click" Width="100" Height="35" 
                    Margin="0,15,0,0" Style="{StaticResource SidebarButton}" HorizontalAlignment="Right"/>
        </Grid>
    </Border>
</Window>
